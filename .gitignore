# Local .terraform directories
.terraform/

# .tfstate files
*.tfstate
*.tfstate.*

# Terraform output dumps (contain account IDs / ARNs — keep local until M1 redact)
bootstrap-outputs.json

# Backend config — contains the dev account ID in the bucket name
backend.hcl

# History-rewrite replacement map — maps real AWS IDs to placeholders
replacements.txt

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version
# control as they are data points which are potentially sensitive and subject
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Ignore saved plan files from `terraform plan -out=tfplan`
tfplan
*.tfplan

# Lambda deployment artifacts produced by archive_file data sources
platform/janitor.zip
labs/modules/probe/probe.zip

# Python bytecode caches from local handler.py compiles
__pycache__/

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Optional: ignore graph output files generated by `terraform graph`
# *.dot

# Optional: ignore plan files saved before destroying Terraform configuration
# Uncomment the line below if you want to ignore planout files.
# planout